# Accuracy Evals

Use this folder for behavior checks that prove the agent or code still does the right thing.

Examples:

- small feature flows
- bug fixes with deterministic inputs and outputs
- table-driven cases for language-specific logic

Keep suites deterministic and fast. If a case depends on external services, stub them or move the test into a safer boundary.
